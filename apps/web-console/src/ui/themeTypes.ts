export type ThemeMode = "light" | "dark";

export type ThemeTokens = {
  bg: string;
  panel: string;
  panelAlt: string;
  border: string;
  borderSoft: string;
  text: string;
  textMuted: string;
  primary: string;
  primarySoft: string;
  danger: string;
  success: string;
  shadow: string;
  inset: string;
};
